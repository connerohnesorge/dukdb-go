<!-- Code generated by gomarkdoc. DO NOT EDIT -->

# secret

```go
import "github.com/dukdb/dukdb-go/internal/secret"
```

Package secret provides secret management for cloud storage credentials.

Package secret provides secret management for cloud storage credentials.

The secret package implements DuckDB\-compatible secret storage with support for multiple cloud providers \(S3, GCS, Azure, HTTP\) and path\-based scope matching.

### Secret Types

Secrets can be created for different cloud storage providers:

- S3: Amazon S3 and S3\-compatible storage
- GCS: Google Cloud Storage
- Azure: Azure Blob Storage
- HTTP: HTTP/HTTPS URLs with authentication
- HuggingFace: HuggingFace Hub credentials

### Credential Providers

Secrets support different credential provider types:

- CONFIG: Explicitly provided credentials \(access key, secret key\)
- ENV: Credentials from environment variables
- CREDENTIAL\_CHAIN: Default credential provider chain
- IAM: IAM role credentials \(from instance metadata\)

### Scope Matching

Secrets use path\-based scope matching to determine which secret applies to a URL:

- GLOBAL: Matches all URLs of the secret type
- PATH: Matches URLs with a specific prefix \(e.g., s3://bucket/path\)
- HOST: Matches URLs with a specific host/bucket

When multiple secrets match a URL, the most specific match \(longest prefix\) is used.

### Example Usage

```
// Create a secret manager with in-memory catalog
catalog := secret.NewMemoryCatalog()
manager := secret.NewManager(catalog)

// Create an S3 secret with global scope
err := manager.CreateSecret(ctx, secret.Secret{
    Name:     "my_s3_secret",
    Type:     secret.SecretTypeS3,
    Provider: secret.ProviderConfig,
    Scope:    secret.SecretScope{Type: secret.ScopeGlobal},
    Options: secret.SecretOptions{
        "key_id":  "AKIAIOSFODNN7EXAMPLE",
        "secret":  "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY",
        "region":  "us-west-2",
    },
    Persistent: true,
})

// Create a bucket-specific secret with path scope
err = manager.CreateSecret(ctx, secret.Secret{
    Name:     "bucket_secret",
    Type:     secret.SecretTypeS3,
    Provider: secret.ProviderConfig,
    Scope:    secret.SecretScope{
        Type:   secret.ScopePath,
        Prefix: "s3://my-bucket/data/",
    },
    Options: secret.SecretOptions{
        "key_id":  "AKIAIOSFODNN7OTHER",
        "secret":  "differentKey",
        "region":  "us-east-1",
    },
})

// Get the best matching secret for a URL
// This will return bucket_secret because it has a more specific scope
secret, err := manager.GetSecret(ctx, "s3://my-bucket/data/file.parquet", secret.SecretTypeS3)
```

### Persistence

Secrets can be stored persistently using a Catalog implementation. The MemoryCatalog provides in\-memory storage for testing. Production use would typically use a DuckDB catalog\-backed implementation.

Package secret provides secret management for cloud storage credentials. It implements DuckDB\-compatible secret storage with path\-based scope matching for S3, GCS, Azure, and HTTP credentials.

## Index

- [Constants](<#constants>)
- [Variables](<#variables>)
- [func BindDropSecret\(name string, ifExists bool\) \(string, bool, error\)](<#BindDropSecret>)
- [func extractHost\(url string\) string](<#extractHost>)
- [func isValidSecretType\(t SecretType\) bool](<#isValidSecretType>)
- [func parseINI\(r io.Reader, section string\) \(map\[string\]string, error\)](<#parseINI>)
- [type Catalog](<#Catalog>)
- [type ConfigProvider](<#ConfigProvider>)
  - [func NewConfigProvider\(accessKeyID, secretAccessKey, sessionToken, region string\) \*ConfigProvider](<#NewConfigProvider>)
  - [func NewConfigProviderFromSecret\(s \*Secret\) \*ConfigProvider](<#NewConfigProviderFromSecret>)
  - [func \(\*ConfigProvider\) Name\(\) string](<#ConfigProvider.Name>)
  - [func \(p \*ConfigProvider\) Retrieve\(\_ context.Context\) \(\*Credentials, error\)](<#ConfigProvider.Retrieve>)
- [type CredentialChain](<#CredentialChain>)
  - [func NewCredentialChain\(providers ...CredentialProvider\) \*CredentialChain](<#NewCredentialChain>)
  - [func NewDefaultProviderChain\(\) \*CredentialChain](<#NewDefaultProviderChain>)
  - [func \(\*CredentialChain\) Name\(\) string](<#CredentialChain.Name>)
  - [func \(c \*CredentialChain\) Retrieve\(ctx context.Context\) \(\*Credentials, error\)](<#CredentialChain.Retrieve>)
  - [func \(c \*CredentialChain\) SetLogger\(logger \*slog.Logger\)](<#CredentialChain.SetLogger>)
- [type CredentialProvider](<#CredentialProvider>)
  - [func NewProviderFromSecret\(s \*Secret\) \(CredentialProvider, error\)](<#NewProviderFromSecret>)
- [type Credentials](<#Credentials>)
  - [func ConfigFromSecret\(ctx context.Context, s \*Secret\) \(\*Credentials, error\)](<#ConfigFromSecret>)
  - [func \(c \*Credentials\) IsExpired\(\) bool](<#Credentials.IsExpired>)
  - [func \(c \*Credentials\) IsValid\(\) bool](<#Credentials.IsValid>)
- [type EnvProvider](<#EnvProvider>)
  - [func NewEnvProvider\(\) \*EnvProvider](<#NewEnvProvider>)
  - [func \(\*EnvProvider\) Name\(\) string](<#EnvProvider.Name>)
  - [func \(\*EnvProvider\) Retrieve\(\_ context.Context\) \(\*Credentials, error\)](<#EnvProvider.Retrieve>)
- [type IMDSv2Option](<#IMDSv2Option>)
  - [func WithHTTPClient\(client \*http.Client\) IMDSv2Option](<#WithHTTPClient>)
  - [func WithIMDSEndpoint\(endpoint string\) IMDSv2Option](<#WithIMDSEndpoint>)
  - [func WithTokenTTL\(ttl int\) IMDSv2Option](<#WithTokenTTL>)
- [type IMDSv2Provider](<#IMDSv2Provider>)
  - [func NewIMDSv2Provider\(opts ...IMDSv2Option\) \*IMDSv2Provider](<#NewIMDSv2Provider>)
  - [func \(\*IMDSv2Provider\) Name\(\) string](<#IMDSv2Provider.Name>)
  - [func \(p \*IMDSv2Provider\) Retrieve\(ctx context.Context\) \(\*Credentials, error\)](<#IMDSv2Provider.Retrieve>)
  - [func \(p \*IMDSv2Provider\) getCredentials\(ctx context.Context, token, roleName string\) \(\*Credentials, error\)](<#IMDSv2Provider.getCredentials>)
  - [func \(p \*IMDSv2Provider\) getRegion\(ctx context.Context, token string\) \(string, error\)](<#IMDSv2Provider.getRegion>)
  - [func \(p \*IMDSv2Provider\) getRoleName\(ctx context.Context, token string\) \(string, error\)](<#IMDSv2Provider.getRoleName>)
  - [func \(p \*IMDSv2Provider\) getToken\(ctx context.Context\) \(string, error\)](<#IMDSv2Provider.getToken>)
- [type Manager](<#Manager>)
  - [func NewManager\(catalog Catalog\) Manager](<#NewManager>)
- [type MemoryCatalog](<#MemoryCatalog>)
  - [func NewMemoryCatalog\(\) \*MemoryCatalog](<#NewMemoryCatalog>)
  - [func \(c \*MemoryCatalog\) Clear\(\)](<#MemoryCatalog.Clear>)
  - [func \(c \*MemoryCatalog\) Count\(\) int](<#MemoryCatalog.Count>)
  - [func \(c \*MemoryCatalog\) DeleteSecret\(name string\) error](<#MemoryCatalog.DeleteSecret>)
  - [func \(c \*MemoryCatalog\) GetSecret\(name string\) \(\*Secret, error\)](<#MemoryCatalog.GetSecret>)
  - [func \(c \*MemoryCatalog\) ListSecrets\(\) \(\[\]Secret, error\)](<#MemoryCatalog.ListSecrets>)
  - [func \(c \*MemoryCatalog\) SetSecret\(s Secret\) error](<#MemoryCatalog.SetSecret>)
- [type ProviderType](<#ProviderType>)
- [type ScopeType](<#ScopeType>)
- [type Secret](<#Secret>)
  - [func BindCreateSecret\(ctx context.Context, name string, secretType string, provider string, scope string, persistent bool, options map\[string\]string\) \(\*Secret, error\)](<#BindCreateSecret>)
  - [func LookupSecretForURL\(ctx context.Context, mgr Manager, url string\) \(\*Secret, error\)](<#LookupSecretForURL>)
  - [func \(s \*Secret\) Clone\(\) \*Secret](<#Secret.Clone>)
  - [func \(s \*Secret\) GetOption\(key string\) string](<#Secret.GetOption>)
  - [func \(s \*Secret\) HasOption\(key string\) bool](<#Secret.HasOption>)
  - [func \(s \*Secret\) SetOption\(key, value string\)](<#Secret.SetOption>)
- [type SecretOptions](<#SecretOptions>)
  - [func BindAlterSecret\(name string, options map\[string\]string\) \(string, SecretOptions, error\)](<#BindAlterSecret>)
- [type SecretScope](<#SecretScope>)
  - [func ParseScope\(scope string\) SecretScope](<#ParseScope>)
- [type SecretType](<#SecretType>)
  - [func SecretTypeFromURL\(url string\) SecretType](<#SecretTypeFromURL>)
- [type SharedConfigOption](<#SharedConfigOption>)
  - [func WithConfigFile\(path string\) SharedConfigOption](<#WithConfigFile>)
  - [func WithCredentialsFile\(path string\) SharedConfigOption](<#WithCredentialsFile>)
  - [func WithProfile\(profile string\) SharedConfigOption](<#WithProfile>)
- [type SharedConfigProvider](<#SharedConfigProvider>)
  - [func NewSharedConfigProvider\(opts ...SharedConfigOption\) \*SharedConfigProvider](<#NewSharedConfigProvider>)
  - [func \(\*SharedConfigProvider\) Name\(\) string](<#SharedConfigProvider.Name>)
  - [func \(p \*SharedConfigProvider\) Retrieve\(\_ context.Context\) \(\*Credentials, error\)](<#SharedConfigProvider.Retrieve>)
  - [func \(\*SharedConfigProvider\) readINIFile\(filename, section string\) \(map\[string\]string, error\)](<#SharedConfigProvider.readINIFile>)
- [type imdsCredentialsResponse](<#imdsCredentialsResponse>)
- [type manager](<#manager>)
  - [func \(m \*manager\) AlterSecret\(\_ context.Context, name string, opts SecretOptions\) error](<#manager.AlterSecret>)
  - [func \(m \*manager\) CreateSecret\(\_ context.Context, s Secret\) error](<#manager.CreateSecret>)
  - [func \(m \*manager\) DropSecret\(\_ context.Context, name string, ifExists bool\) error](<#manager.DropSecret>)
  - [func \(m \*manager\) GetSecret\(\_ context.Context, url string, secretType SecretType\) \(\*Secret, error\)](<#manager.GetSecret>)
  - [func \(m \*manager\) GetSecretByName\(\_ context.Context, name string\) \(\*Secret, error\)](<#manager.GetSecretByName>)
  - [func \(m \*manager\) ListSecrets\(\_ context.Context, scope SecretScope\) \(\[\]Secret, error\)](<#manager.ListSecrets>)
  - [func \(m \*manager\) LoadFromCatalog\(\) error](<#manager.LoadFromCatalog>)
  - [func \(\*manager\) matchScope\(scope SecretScope, url string\) int](<#manager.matchScope>)
  - [func \(\*manager\) scopeMatches\(secretScope, filterScope SecretScope\) bool](<#manager.scopeMatches>)


## Constants

<a name="EnvAccessKeyID"></a>AWS environment variable names.

```go
const (
    EnvAccessKeyID     = "AWS_ACCESS_KEY_ID"
    EnvSecretAccessKey = "AWS_SECRET_ACCESS_KEY"
    EnvSessionToken    = "AWS_SESSION_TOKEN"
    EnvRegion          = "AWS_REGION"
    EnvDefaultRegion   = "AWS_DEFAULT_REGION"
)
```

<a name="DefaultIMDSEndpoint"></a>IMDS constants.

```go
const (
    // DefaultIMDSEndpoint is the default EC2 metadata service endpoint.
    DefaultIMDSEndpoint = "http://169.254.169.254"
    // IMDSTokenPath is the path to get an IMDSv2 token.
    IMDSTokenPath = "/latest/api/token"
    // IMDSCredentialsPath is the path to get IAM credentials.
    IMDSCredentialsPath = "/latest/meta-data/iam/security-credentials/"
    // IMDSRegionPath is the path to get the instance region.
    IMDSRegionPath = "/latest/meta-data/placement/region"
    // DefaultIMDSTokenTTL is the default token TTL in seconds.
    DefaultIMDSTokenTTL = 21600 // 6 hours
    // DefaultIMDSTimeout is the default timeout for IMDS requests.
    DefaultIMDSTimeout = 2 * time.Second
)
```

<a name="OptionKeyID"></a>Common secret option keys.

```go
const (
    // S3/AWS options
    OptionKeyID        = "key_id"
    OptionSecret       = "secret"
    OptionSessionToken = "session_token"
    OptionRegion       = "region"
    OptionEndpoint     = "endpoint"
    OptionURLStyle     = "url_style"
    OptionUseSsl       = "use_ssl"

    // Azure options
    OptionAccountName      = "account_name"
    OptionAccountKey       = "account_key"
    OptionConnectionString = "connection_string"
    OptionTenantID         = "tenant_id"
    OptionClientID         = "client_id"
    OptionClientSecret     = "client_secret"

    // GCS options
    OptionServiceAccountJSON = "service_account_json"
    OptionProjectID          = "project_id"

    // HTTP options
    OptionBearerToken  = "bearer_token"
    OptionExtraHeaders = "extra_http_headers"
)
```

<a name="errUnexpectedStatusCode"></a>errUnexpectedStatusCode is the format string for unexpected HTTP status codes.

```go
const errUnexpectedStatusCode = "unexpected status code: %d"
```

<a name="schemeSeparatorLen"></a>schemeSeparatorLen is the length of "://".

```go
const schemeSeparatorLen = 3
```

## Variables

<a name="ErrSecretNotFound"></a>Common errors for secret operations.

```go
var (
    // ErrSecretNotFound is returned when a secret cannot be found.
    ErrSecretNotFound = errors.New("secret not found")
    // ErrSecretExists is returned when attempting to create a secret that already exists.
    ErrSecretExists = errors.New("secret already exists")
    // ErrInvalidSecretName is returned when a secret name is empty or invalid.
    ErrInvalidSecretName = errors.New("invalid secret name")
    // ErrInvalidSecretType is returned when a secret type is invalid.
    ErrInvalidSecretType = errors.New("invalid secret type")
)
```

<a name="ErrNoCredentials"></a>Common errors for credential providers.

```go
var (
    // ErrNoCredentials is returned when a provider cannot retrieve credentials.
    ErrNoCredentials = errors.New("no credentials available")
    // ErrCredentialsExpired is returned when credentials have expired.
    ErrCredentialsExpired = errors.New("credentials expired")
    // ErrProviderNotSupported is returned when a provider type is not supported.
    ErrProviderNotSupported = errors.New("provider type not supported")
)
```

<a name="BindDropSecret"></a>
## func [BindDropSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/binder.go#L65>)

```go
func BindDropSecret(name string, ifExists bool) (string, bool, error)
```

BindDropSecret validates a DROP SECRET statement. It returns the secret name and whether IF EXISTS was specified.

<a name="extractHost"></a>
## func [extractHost](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L322>)

```go
func extractHost(url string) string
```

extractHost extracts the host/bucket from a URL.

<a name="isValidSecretType"></a>
## func [isValidSecretType](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/binder.go#L134>)

```go
func isValidSecretType(t SecretType) bool
```

isValidSecretType checks if a secret type is valid.

<a name="parseINI"></a>
## func [parseINI](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L301>)

```go
func parseINI(r io.Reader, section string) (map[string]string, error)
```

parseINI parses an INI file and returns values for the specified section.

<a name="Catalog"></a>
## type [Catalog](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/catalog.go#L8-L17>)

Catalog provides persistent storage for secrets.

```go
type Catalog interface {
    // GetSecret retrieves a secret by name from persistent storage.
    GetSecret(name string) (*Secret, error)
    // SetSecret stores or updates a secret in persistent storage.
    SetSecret(s Secret) error
    // DeleteSecret removes a secret from persistent storage.
    DeleteSecret(name string) error
    // ListSecrets retrieves all secrets from persistent storage.
    ListSecrets() ([]Secret, error)
}
```

<a name="_"></a>Verify MemoryCatalog implements Catalog interface.

```go
var _ Catalog = (*MemoryCatalog)(nil)
```

<a name="ConfigProvider"></a>
## type [ConfigProvider](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L70-L72>)

ConfigProvider provides static credentials from a secret's options.

```go
type ConfigProvider struct {
    credentials Credentials
}
```

<a name="NewConfigProvider"></a>
### func [NewConfigProvider](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L75>)

```go
func NewConfigProvider(accessKeyID, secretAccessKey, sessionToken, region string) *ConfigProvider
```

NewConfigProvider creates a ConfigProvider from static credentials.

<a name="NewConfigProviderFromSecret"></a>
### func [NewConfigProviderFromSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L87>)

```go
func NewConfigProviderFromSecret(s *Secret) *ConfigProvider
```

NewConfigProviderFromSecret creates a ConfigProvider from a Secret's options.

<a name="ConfigProvider.Name"></a>
### func \(\*ConfigProvider\) [Name](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L114>)

```go
func (*ConfigProvider) Name() string
```

Name returns the provider name.

<a name="ConfigProvider.Retrieve"></a>
### func \(\*ConfigProvider\) [Retrieve](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L99>)

```go
func (p *ConfigProvider) Retrieve(_ context.Context) (*Credentials, error)
```

Retrieve returns the static credentials.

<a name="CredentialChain"></a>
## type [CredentialChain](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L603-L608>)

CredentialChain tries multiple providers in order until one succeeds.

```go
type CredentialChain struct {
    // providers is the list of providers to try in order.
    providers []CredentialProvider
    // logger is used for logging which provider succeeded.
    logger *slog.Logger
}
```

<a name="NewCredentialChain"></a>
### func [NewCredentialChain](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L611>)

```go
func NewCredentialChain(providers ...CredentialProvider) *CredentialChain
```

NewCredentialChain creates a CredentialChain with the given providers.

<a name="NewDefaultProviderChain"></a>
### func [NewDefaultProviderChain](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L659>)

```go
func NewDefaultProviderChain() *CredentialChain
```

NewDefaultProviderChain creates a default credential provider chain. The chain tries providers in this order: 1. Environment variables \(AWS\_ACCESS\_KEY\_ID, AWS\_SECRET\_ACCESS\_KEY\) 2. Shared credentials file \(\~/.aws/credentials\) 3. EC2 instance metadata \(IMDSv2\)

<a name="CredentialChain.Name"></a>
### func \(\*CredentialChain\) [Name](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L650>)

```go
func (*CredentialChain) Name() string
```

Name returns the provider name.

<a name="CredentialChain.Retrieve"></a>
### func \(\*CredentialChain\) [Retrieve](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L624>)

```go
func (c *CredentialChain) Retrieve(ctx context.Context) (*Credentials, error)
```

Retrieve tries each provider in order until one succeeds.

<a name="CredentialChain.SetLogger"></a>
### func \(\*CredentialChain\) [SetLogger](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L619>)

```go
func (c *CredentialChain) SetLogger(logger *slog.Logger)
```

SetLogger sets the logger for the credential chain.

<a name="CredentialProvider"></a>
## type [CredentialProvider](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L62-L67>)

CredentialProvider provides credentials for cloud services.

```go
type CredentialProvider interface {
    // Retrieve returns credentials or an error.
    Retrieve(ctx context.Context) (*Credentials, error)
    // Name returns the provider name for logging.
    Name() string
}
```

<a name="_"></a>Verify interface implementations.

```go
var (
    _   CredentialProvider = (*ConfigProvider)(nil)
    _   CredentialProvider = (*EnvProvider)(nil)
    _   CredentialProvider = (*SharedConfigProvider)(nil)
    _   CredentialProvider = (*IMDSv2Provider)(nil)
    _   CredentialProvider = (*CredentialChain)(nil)
)
```

<a name="NewProviderFromSecret"></a>
### func [NewProviderFromSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L668>)

```go
func NewProviderFromSecret(s *Secret) (CredentialProvider, error)
```

NewProviderFromSecret creates a CredentialProvider based on a secret's provider type.

<a name="Credentials"></a>
## type [Credentials](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L32-L44>)

Credentials holds AWS\-style credentials retrieved from a provider.

```go
type Credentials struct {
    // AccessKeyID is the AWS access key ID.
    AccessKeyID string
    // SecretAccessKey is the AWS secret access key.
    SecretAccessKey string
    // SessionToken is an optional session token for temporary credentials.
    SessionToken string
    // Region is the AWS region.
    Region string
    // Expiration is the time when these credentials expire.
    // Zero time means no expiration.
    Expiration time.Time
}
```

<a name="ConfigFromSecret"></a>
### func [ConfigFromSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L708>)

```go
func ConfigFromSecret(ctx context.Context, s *Secret) (*Credentials, error)
```

ConfigFromSecret creates an S3Config from a secret using the appropriate provider. This is a helper function for integrating with the S3 filesystem.

<a name="Credentials.IsExpired"></a>
### func \(\*Credentials\) [IsExpired](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L48>)

```go
func (c *Credentials) IsExpired() bool
```

IsExpired returns true if the credentials have expired. Returns false if Expiration is zero \(no expiration\).

<a name="Credentials.IsValid"></a>
### func \(\*Credentials\) [IsValid](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L57>)

```go
func (c *Credentials) IsValid() bool
```

IsValid returns true if the credentials have the minimum required fields.

<a name="EnvProvider"></a>
## type [EnvProvider](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L119>)

EnvProvider reads credentials from environment variables.

```go
type EnvProvider struct{}
```

<a name="NewEnvProvider"></a>
### func [NewEnvProvider](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L131>)

```go
func NewEnvProvider() *EnvProvider
```

NewEnvProvider creates an EnvProvider.

<a name="EnvProvider.Name"></a>
### func \(\*EnvProvider\) [Name](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L163>)

```go
func (*EnvProvider) Name() string
```

Name returns the provider name.

<a name="EnvProvider.Retrieve"></a>
### func \(\*EnvProvider\) [Retrieve](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L136>)

```go
func (*EnvProvider) Retrieve(_ context.Context) (*Credentials, error)
```

Retrieve returns credentials from environment variables.

<a name="IMDSv2Option"></a>
## type [IMDSv2Option](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L360>)

IMDSv2Option is a functional option for IMDSv2Provider.

```go
type IMDSv2Option func(*IMDSv2Provider)
```

<a name="WithHTTPClient"></a>
### func [WithHTTPClient](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L379>)

```go
func WithHTTPClient(client *http.Client) IMDSv2Option
```

WithHTTPClient sets the HTTP client to use.

<a name="WithIMDSEndpoint"></a>
### func [WithIMDSEndpoint](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L386>)

```go
func WithIMDSEndpoint(endpoint string) IMDSv2Option
```

WithIMDSEndpoint sets the IMDS endpoint.

<a name="WithTokenTTL"></a>
### func [WithTokenTTL](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L393>)

```go
func WithTokenTTL(ttl int) IMDSv2Option
```

WithTokenTTL sets the token TTL in seconds.

<a name="IMDSv2Provider"></a>
## type [IMDSv2Provider](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L350-L357>)

IMDSv2Provider gets credentials from EC2 instance metadata service \(IMDSv2\).

```go
type IMDSv2Provider struct {
    // httpClient is the HTTP client to use for requests.
    httpClient *http.Client
    // endpoint is the IMDS endpoint. Defaults to the EC2 metadata endpoint.
    endpoint string
    // tokenTTL is the TTL for the IMDSv2 token in seconds.
    tokenTTL int
}
```

<a name="NewIMDSv2Provider"></a>
### func [NewIMDSv2Provider](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L400>)

```go
func NewIMDSv2Provider(opts ...IMDSv2Option) *IMDSv2Provider
```

NewIMDSv2Provider creates an IMDSv2Provider.

<a name="IMDSv2Provider.Name"></a>
### func \(\*IMDSv2Provider\) [Name](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L598>)

```go
func (*IMDSv2Provider) Name() string
```

Name returns the provider name.

<a name="IMDSv2Provider.Retrieve"></a>
### func \(\*IMDSv2Provider\) [Retrieve](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L417>)

```go
func (p *IMDSv2Provider) Retrieve(ctx context.Context) (*Credentials, error)
```

Retrieve returns credentials from EC2 instance metadata.

<a name="IMDSv2Provider.getCredentials"></a>
### func \(\*IMDSv2Provider\) [getCredentials](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L524-L527>)

```go
func (p *IMDSv2Provider) getCredentials(ctx context.Context, token, roleName string) (*Credentials, error)
```

getCredentials retrieves credentials for the specified IAM role.

<a name="IMDSv2Provider.getRegion"></a>
### func \(\*IMDSv2Provider\) [getRegion](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L567>)

```go
func (p *IMDSv2Provider) getRegion(ctx context.Context, token string) (string, error)
```

getRegion retrieves the instance region from metadata.

<a name="IMDSv2Provider.getRoleName"></a>
### func \(\*IMDSv2Provider\) [getRoleName](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L476>)

```go
func (p *IMDSv2Provider) getRoleName(ctx context.Context, token string) (string, error)
```

getRoleName retrieves the IAM role name from instance metadata.

<a name="IMDSv2Provider.getToken"></a>
### func \(\*IMDSv2Provider\) [getToken](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L445>)

```go
func (p *IMDSv2Provider) getToken(ctx context.Context) (string, error)
```

getToken retrieves an IMDSv2 token.

<a name="Manager"></a>
## type [Manager](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L27-L53>)

Manager provides operations for managing secrets.

```go
type Manager interface {
    // CreateSecret creates a new secret.
    // Returns ErrSecretExists if a secret with the same name already exists.
    CreateSecret(ctx context.Context, s Secret) error

    // DropSecret removes a secret by name.
    // If ifExists is true, no error is returned if the secret doesn't exist.
    // Returns ErrSecretNotFound if the secret doesn't exist and ifExists is false.
    DropSecret(ctx context.Context, name string, ifExists bool) error

    // AlterSecret updates the options of an existing secret.
    // Returns ErrSecretNotFound if the secret doesn't exist.
    AlterSecret(ctx context.Context, name string, opts SecretOptions) error

    // GetSecret finds the best matching secret for a URL and secret type.
    // It uses longest prefix match to find the most specific matching secret.
    // Returns ErrSecretNotFound if no matching secret is found.
    GetSecret(ctx context.Context, url string, secretType SecretType) (*Secret, error)

    // GetSecretByName retrieves a secret by its name.
    // Returns ErrSecretNotFound if the secret doesn't exist.
    GetSecretByName(ctx context.Context, name string) (*Secret, error)

    // ListSecrets lists all secrets, optionally filtered by scope.
    // If scope.Type is empty, all secrets are returned.
    ListSecrets(ctx context.Context, scope SecretScope) ([]Secret, error)
}
```

<a name="_"></a>Verify manager implements Manager interface.

```go
var _ Manager = (*manager)(nil)
```

<a name="NewManager"></a>
### func [NewManager](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L64>)

```go
func NewManager(catalog Catalog) Manager
```

NewManager creates a new secret manager. If catalog is nil, secrets are only stored in memory.

<a name="MemoryCatalog"></a>
## type [MemoryCatalog](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/catalog.go#L23-L26>)

MemoryCatalog implements Catalog using in\-memory storage. This is useful for testing or when persistence is not required. For actual database persistence, a DuckDB\-backed implementation would store secrets in the catalog tables.

```go
type MemoryCatalog struct {
    mu      sync.RWMutex
    secrets map[string]Secret
}
```

<a name="NewMemoryCatalog"></a>
### func [NewMemoryCatalog](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/catalog.go#L29>)

```go
func NewMemoryCatalog() *MemoryCatalog
```

NewMemoryCatalog creates a new in\-memory catalog.

<a name="MemoryCatalog.Clear"></a>
### func \(\*MemoryCatalog\) [Clear](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/catalog.go#L87>)

```go
func (c *MemoryCatalog) Clear()
```

Clear removes all secrets from the catalog.

<a name="MemoryCatalog.Count"></a>
### func \(\*MemoryCatalog\) [Count](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/catalog.go#L95>)

```go
func (c *MemoryCatalog) Count() int
```

Count returns the number of secrets in the catalog.

<a name="MemoryCatalog.DeleteSecret"></a>
### func \(\*MemoryCatalog\) [DeleteSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/catalog.go#L59>)

```go
func (c *MemoryCatalog) DeleteSecret(name string) error
```

DeleteSecret removes a secret.

<a name="MemoryCatalog.GetSecret"></a>
### func \(\*MemoryCatalog\) [GetSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/catalog.go#L36>)

```go
func (c *MemoryCatalog) GetSecret(name string) (*Secret, error)
```

GetSecret retrieves a secret by name.

<a name="MemoryCatalog.ListSecrets"></a>
### func \(\*MemoryCatalog\) [ListSecrets](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/catalog.go#L73>)

```go
func (c *MemoryCatalog) ListSecrets() ([]Secret, error)
```

ListSecrets retrieves all secrets.

<a name="MemoryCatalog.SetSecret"></a>
### func \(\*MemoryCatalog\) [SetSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/catalog.go#L49>)

```go
func (c *MemoryCatalog) SetSecret(s Secret) error
```

SetSecret stores or updates a secret.

<a name="ProviderType"></a>
## type [ProviderType](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/types.go#L23>)

ProviderType represents the credential provider type.

```go
type ProviderType string
```

<a name="ProviderConfig"></a>Supported credential provider types.

```go
const (
    // ProviderConfig uses explicitly provided credentials (access key, secret key).
    ProviderConfig ProviderType = "CONFIG"
    // ProviderEnv uses credentials from environment variables.
    ProviderEnv ProviderType = "ENV"
    // ProviderCredentialChain uses the default credential provider chain.
    ProviderCredentialChain ProviderType = "CREDENTIAL_CHAIN"
    // ProviderIAM uses IAM role credentials (e.g., from EC2 instance metadata).
    ProviderIAM ProviderType = "IAM"
)
```

<a name="ScopeType"></a>
## type [ScopeType](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/types.go#L38>)

ScopeType represents the type of secret scope.

```go
type ScopeType string
```

<a name="ScopeGlobal"></a>Supported scope types for secret matching.

```go
const (
    // ScopeGlobal matches all URLs of the secret type.
    ScopeGlobal ScopeType = "GLOBAL"
    // ScopePath matches URLs with a specific path prefix (e.g., s3://bucket/path).
    ScopePath ScopeType = "PATH"
    // ScopeHost matches URLs with a specific host (e.g., s3://bucket).
    ScopeHost ScopeType = "HOST"
)
```

<a name="Secret"></a>
## type [Secret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/types.go#L72-L89>)

Secret represents a stored credential secret.

```go
type Secret struct {
    // Name is the unique identifier for the secret.
    Name string
    // Type is the type of cloud service (S3, GCS, AZURE, HTTP, HUGGINGFACE).
    Type SecretType
    // Provider is the credential provider type (CONFIG, ENV, CREDENTIAL_CHAIN, IAM).
    Provider ProviderType
    // Scope defines the URL scope this secret applies to.
    Scope SecretScope
    // Options contains the secret configuration options.
    Options SecretOptions
    // Persistent indicates whether the secret should be stored persistently.
    Persistent bool
    // CreatedAt is the time the secret was created.
    CreatedAt time.Time
    // UpdatedAt is the time the secret was last updated.
    UpdatedAt time.Time
}
```

<a name="BindCreateSecret"></a>
### func [BindCreateSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/binder.go#L13-L21>)

```go
func BindCreateSecret(ctx context.Context, name string, secretType string, provider string, scope string, persistent bool, options map[string]string) (*Secret, error)
```

BindCreateSecret binds a CREATE SECRET statement to a Secret. It parses the statement options and creates a Secret object that can be stored in the SecretManager.

<a name="LookupSecretForURL"></a>
### func [LookupSecretForURL](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/binder.go#L145>)

```go
func LookupSecretForURL(ctx context.Context, mgr Manager, url string) (*Secret, error)
```

LookupSecretForURL finds the best matching secret for a given URL. This is a helper function that wraps the Manager's GetSecret method.

<a name="Secret.Clone"></a>
### func \(\*Secret\) [Clone](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/types.go#L92>)

```go
func (s *Secret) Clone() *Secret
```

Clone creates a deep copy of the secret.

<a name="Secret.GetOption"></a>
### func \(\*Secret\) [GetOption](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/types.go#L112>)

```go
func (s *Secret) GetOption(key string) string
```

GetOption returns the value of an option, or empty string if not set.

<a name="Secret.HasOption"></a>
### func \(\*Secret\) [HasOption](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/types.go#L130>)

```go
func (s *Secret) HasOption(key string) bool
```

HasOption returns true if the option is set.

<a name="Secret.SetOption"></a>
### func \(\*Secret\) [SetOption](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/types.go#L121>)

```go
func (s *Secret) SetOption(key, value string)
```

SetOption sets an option value.

<a name="SecretOptions"></a>
## type [SecretOptions](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/types.go#L69>)

SecretOptions contains key\-value pairs of secret configuration options. Common options include:

- key\_id: AWS access key ID
- secret: AWS secret access key
- session\_token: AWS session token
- region: AWS region
- endpoint: Custom endpoint URL
- account\_name: Azure storage account name
- account\_key: Azure storage account key
- connection\_string: Azure connection string
- service\_account\_json: GCS service account JSON

```go
type SecretOptions map[string]string
```

<a name="BindAlterSecret"></a>
### func [BindAlterSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/binder.go#L75>)

```go
func BindAlterSecret(name string, options map[string]string) (string, SecretOptions, error)
```

BindAlterSecret validates an ALTER SECRET statement and returns the options to update.

<a name="SecretScope"></a>
## type [SecretScope](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/types.go#L51-L56>)

SecretScope defines the scope for which a secret applies.

```go
type SecretScope struct {
    // Type is the scope type (GLOBAL, PATH, or HOST).
    Type ScopeType
    // Prefix is the URL prefix this scope matches (for PATH and HOST scopes).
    Prefix string
}
```

<a name="ParseScope"></a>
### func [ParseScope](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/binder.go#L100>)

```go
func ParseScope(scope string) SecretScope
```

ParseScope parses a scope string and returns a SecretScope. Supported formats:

- Empty string: Global scope
- "s3://bucket/path": Path scope for S3
- "gs://bucket/path": Path scope for GCS
- "azure://container/path": Path scope for Azure
- "https://example.com/path": Path scope for HTTP
- Other: Host scope

<a name="SecretType"></a>
## type [SecretType](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/types.go#L11>)

SecretType represents the type of cloud service the secret is for.

```go
type SecretType string
```

<a name="SecretTypeS3"></a>Supported secret types for cloud storage.

```go
const (
    SecretTypeS3          SecretType = "S3"
    SecretTypeHTTP        SecretType = "HTTP"
    SecretTypeAzure       SecretType = "AZURE"
    SecretTypeGCS         SecretType = "GCS"
    SecretTypeHuggingFace SecretType = "HUGGINGFACE"
)
```

<a name="SecretTypeFromURL"></a>
### func [SecretTypeFromURL](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/binder.go#L156>)

```go
func SecretTypeFromURL(url string) SecretType
```

SecretTypeFromURL determines the secret type from a URL.

<a name="SharedConfigOption"></a>
## type [SharedConfigOption](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L180>)

SharedConfigOption is a functional option for SharedConfigProvider.

```go
type SharedConfigOption func(*SharedConfigProvider)
```

<a name="WithConfigFile"></a>
### func [WithConfigFile](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L197>)

```go
func WithConfigFile(path string) SharedConfigOption
```

WithConfigFile sets the path to the config file.

<a name="WithCredentialsFile"></a>
### func [WithCredentialsFile](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L190>)

```go
func WithCredentialsFile(path string) SharedConfigOption
```

WithCredentialsFile sets the path to the credentials file.

<a name="WithProfile"></a>
### func [WithProfile](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L183>)

```go
func WithProfile(profile string) SharedConfigOption
```

WithProfile sets the AWS profile to use.

<a name="SharedConfigProvider"></a>
## type [SharedConfigProvider](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L168-L177>)

SharedConfigProvider reads credentials from AWS CLI config files.

```go
type SharedConfigProvider struct {
    // profile is the AWS profile name to use. Defaults to "default".
    profile string
    // credentialsFile is the path to the credentials file.
    // Defaults to ~/.aws/credentials.
    credentialsFile string
    // configFile is the path to the config file.
    // Defaults to ~/.aws/config.
    configFile string
}
```

<a name="NewSharedConfigProvider"></a>
### func [NewSharedConfigProvider](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L204>)

```go
func NewSharedConfigProvider(opts ...SharedConfigOption) *SharedConfigProvider
```

NewSharedConfigProvider creates a SharedConfigProvider.

<a name="SharedConfigProvider.Name"></a>
### func \(\*SharedConfigProvider\) [Name](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L345>)

```go
func (*SharedConfigProvider) Name() string
```

Name returns the provider name.

<a name="SharedConfigProvider.Retrieve"></a>
### func \(\*SharedConfigProvider\) [Retrieve](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L239>)

```go
func (p *SharedConfigProvider) Retrieve(_ context.Context) (*Credentials, error)
```

Retrieve returns credentials from AWS CLI config files.

<a name="SharedConfigProvider.readINIFile"></a>
### func \(\*SharedConfigProvider\) [readINIFile](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L288>)

```go
func (*SharedConfigProvider) readINIFile(filename, section string) (map[string]string, error)
```

readINIFile reads an INI file and returns the values for the specified section.

<a name="imdsCredentialsResponse"></a>
## type [imdsCredentialsResponse](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/provider.go#L513-L521>)

imdsCredentialsResponse represents the JSON response from IMDS credentials endpoint.

```go
type imdsCredentialsResponse struct {
    Code            string    `json:"Code"`
    LastUpdated     string    `json:"LastUpdated"`
    Type            string    `json:"Type"`
    AccessKeyID     string    `json:"AccessKeyId"`
    SecretAccessKey string    `json:"SecretAccessKey"`
    Token           string    `json:"Token"`
    Expiration      time.Time `json:"Expiration"`
}
```

<a name="manager"></a>
## type [manager](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L56-L60>)

manager implements the Manager interface.

```go
type manager struct {
    mu      sync.RWMutex
    secrets map[string]Secret
    catalog Catalog
}
```

<a name="manager.AlterSecret"></a>
### func \(\*manager\) [AlterSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L153>)

```go
func (m *manager) AlterSecret(_ context.Context, name string, opts SecretOptions) error
```

AlterSecret updates the options of an existing secret.

<a name="manager.CreateSecret"></a>
### func \(\*manager\) [CreateSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L72>)

```go
func (m *manager) CreateSecret(_ context.Context, s Secret) error
```

CreateSecret creates a new secret.

<a name="manager.DropSecret"></a>
### func \(\*manager\) [DropSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L119>)

```go
func (m *manager) DropSecret(_ context.Context, name string, ifExists bool) error
```

DropSecret removes a secret by name.

<a name="manager.GetSecret"></a>
### func \(\*manager\) [GetSecret](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L197>)

```go
func (m *manager) GetSecret(_ context.Context, url string, secretType SecretType) (*Secret, error)
```

GetSecret finds the best matching secret for a URL and secret type.

<a name="manager.GetSecretByName"></a>
### func \(\*manager\) [GetSecretByName](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L234>)

```go
func (m *manager) GetSecretByName(_ context.Context, name string) (*Secret, error)
```

GetSecretByName retrieves a secret by its name.

<a name="manager.ListSecrets"></a>
### func \(\*manager\) [ListSecrets](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L251>)

```go
func (m *manager) ListSecrets(_ context.Context, scope SecretScope) ([]Secret, error)
```

ListSecrets lists all secrets, optionally filtered by scope.

<a name="manager.LoadFromCatalog"></a>
### func \(\*manager\) [LoadFromCatalog](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L338>)

```go
func (m *manager) LoadFromCatalog() error
```

LoadFromCatalog loads all persistent secrets from the catalog into memory.

<a name="manager.matchScope"></a>
### func \(\*manager\) [matchScope](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L273>)

```go
func (*manager) matchScope(scope SecretScope, url string) int
```

matchScope returns the match length for a scope against a URL. Returns \-1 if there's no match, 0 for global scope, or the prefix length for path/host scope.

<a name="manager.scopeMatches"></a>
### func \(\*manager\) [scopeMatches](<https://github.com/connerohnesorge/dukdb-go/blob/main/internal/secret/manager.go#L305>)

```go
func (*manager) scopeMatches(secretScope, filterScope SecretScope) bool
```

scopeMatches checks if a secret's scope matches the filter scope.

Generated by [gomarkdoc](<https://github.com/princjef/gomarkdoc>)
