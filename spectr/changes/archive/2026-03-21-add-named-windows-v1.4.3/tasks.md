# Tasks: Add Named Window Definitions (WINDOW Clause)

- [ ] 1. Add WindowDef and WindowSpec AST types — In `internal/parser/ast.go`, after line 1122 (WindowExpr), add `WindowDef` struct with `Name string` and `Spec *WindowSpec` fields. Add `WindowSpec` struct with `RefName string`, `PartitionBy []Expr`, `OrderBy []WindowOrderBy`, `Frame *WindowFrame`. Add `Windows []WindowDef` field to `SelectStmt` (line 34-53). Add `RefName string` field to `WindowExpr` (line 1114). Validate: AST compiles with no errors.

- [ ] 2. Parse WINDOW clause in SELECT — In `parseSelect()` at `internal/parser/parser.go`, after QUALIFY parsing (line 384) and before ORDER BY (line 386), add check for WINDOW keyword. Call new `parseWindowDefs()` function that parses `WINDOW name AS (spec) [, name AS (spec) ...]`. Add `parseWindowSpecForDef(spec *WindowSpec)` that reuses existing `parseWindowExprList()` (line 5366) and `parseWindowOrderBy()` (line 5399) helpers. Add "WINDOW" to keyword stop conditions at lines 654, 686, 864, 903, 952. Validate: `SELECT x FROM t WINDOW w AS (ORDER BY x)` parses without error and produces SelectStmt.Windows with one entry.

- [ ] 3. Support OVER with named reference — In `maybeParseWindowExpr()` at `parser.go:5306`, after consuming OVER, check for bare identifier (tokenIdent) for `OVER w` syntax. Also check for identifier inside parens for `OVER (w ORDER BY ...)` inheritance syntax. Set `WindowExpr.RefName` accordingly. Validate: `SELECT ROW_NUMBER() OVER w FROM t WINDOW w AS (ORDER BY x)` parses with RefName="w". `SELECT SUM(x) OVER (w ORDER BY y)` parses with RefName="w" and OrderBy populated.

- [ ] 4. Resolve named windows in binder — Add `windowDefs map[string]*parser.WindowSpec` field to Binder struct. In `bindSelect()` (bind_stmt.go), collect window defs into map, check for duplicates, resolve transitive references with cycle detection. In `bindWindowExpr()` (bind_expr.go:785), when RefName is set, look up named window and merge spec: copy PartitionBy/OrderBy/Frame from base if not set inline. Error on override of existing clauses. Error on undefined or circular references. Validate: `SELECT ROW_NUMBER() OVER w FROM t WINDOW w AS (ORDER BY x)` binds correctly.

- [ ] 5. Integration tests — Write tests covering: (1) named window with multiple functions referencing it, (2) window inheritance adding ORDER BY to base, (3) multiple named windows in same query, (4) transitive window references, (5) error on undefined window name, (6) error on circular window reference, (7) error on overriding base window's ORDER BY, (8) named window with frame specification, (9) subquery with its own WINDOW clause. Verify no regressions in existing window function tests.
